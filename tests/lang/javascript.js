// line comment

/* block comment */

function greet(name) {
    const msg = `Hello, ${name}! // not a comment`;
    const fake = "/* not a block */";
    // real comment
    return msg;
}

/*
 * multi-line block comment
 */
const x = 42;
